package com.sahabt;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import sun.security.krb5.internal.TGSRep;

import java.sql.SQLOutput;
import java.util.Scanner;

public class Selenium {

    public static void main(String[] Args) throws InterruptedException {
       /* WebDriver driver = new ChromeDriver();

        driver.get("https://www.hepsiburada.com/");

        System.out.println(driver.getTitle());

       /* WebElement ed = driver.findElement(By.id("productSearch"));
        ed.sendKeys("Samsung");

        System.out.println(ed.hashCode());
        WebElement ed = driver.findElement(By.id("myAccount"));
        ed.click();






        //Thread.sleep(1500);


        Thread.sleep(1500);

        driver.findElement(By.id("firstname")).sendKeys("ahmet");
        driver.findElement(By.id("lastname")).sendKeys("demirel");
        driver.findElement(By.id("email-register")).sendKeys("demirel@199545demirel.com");
        driver.findElement(By.id("password-register")).sendKeys("demir234");
        driver.findElement(By.className("showPassword")).click();
        driver.findElement(By.className("notification-row")).click();
        driver.findElement(By.className("form-actions")).click();
        Thread.sleep(5000);


        WebElement error = driver.findElement(By.id("error-message-container"));


        if (error.equals("Bu e-posta adresi kullanılamaz. Lütfen başka bir e-posta adresi deneyiniz.")); {
            System.out.println("if error");
            driver.navigate().refresh();
            driver.findElement(By.id("email-register")).sendKeys("demirel@ajsdg h.com");
        }

*/





    }


}
